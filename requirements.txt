# Core Dependencies
colorama==0.4.6  # Cross-platform colored terminal text
cryptography==42.0.5  # Encryption/decryption
requests==2.31.0  # Blockchain API calls
python-nmap==0.7.1  # Nmap integration (Android-compatible)

# Optional (Only if needed by other modules)
scapy==2.5.0  # Lightweight packet manipulation (Termux-compatible build)
